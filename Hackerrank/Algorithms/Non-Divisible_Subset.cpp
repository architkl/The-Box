#include <iostream>
#include <vector>

using namespace std;

int MAX_N = 100005;
vector <int> v(MAX_N);

int main()
{
	int n, k;
	cin >> n >> k;

	for (int i = 0; i < n; i++)
		cin >> v[i];

	return 0;
}